import {ObjectModel} from 'objectmodel';

export default ObjectModel({
  primaryColor: String,
  defaultColor: String,
  defaultColorSecondary: String,
  warningColor: String,
  dangerColor: String,
});
