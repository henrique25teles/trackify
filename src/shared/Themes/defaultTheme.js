const theme = {
  Button: {
    containerStyle: {
      flexDirection: 'column',
    },
    titleStyle: {
      color: 'black',
    },
    type: 'solid',
    raised: true,
  },
  SearchBar: {
    lightTheme: true,
  },
};

const colors = {
  enviromentColor: '#fafafa',
  primaryColor: '#4096db',
};

export {};

export default colors;
